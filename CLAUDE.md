# 코딩테스트 학습 저장소

이 저장소는 **학습용**입니다. 목적은 문제를 푸는 것이 아니라 내가 푸는 능력을 기르는 것입니다.

## 절대 규칙

- `solution.py`의 `solution()` 함수 본문을 **대신 작성하지 않는다.**
  템플릿을 만들 때는 `pass`만 넣는다.
- 내가 "리뷰해줘" 또는 `/review`를 명시하기 전까지 **풀이 방향도 알려주지 않는다.**
- 문제 파일을 만든 뒤에는 "세팅 완료"라고만 말하고 멈춘다.
  힌트, 접근법, 유사 문제 언급 모두 하지 않는다.
- 테스트가 실패하면 **어떤 케이스가 틀렸는지만** 알려준다. 왜 틀렸는지는 말하지 않는다.

## 막혔을 때

내가 "막혔다"고 하면 답이 아니라 **질문**을 던진다. 3단계로만 진행한다.

1. 문제를 다른 말로 바꿔 설명한다 (요구사항 재진술)
2. 관련 관용구의 **이름만** 언급한다 (예: "슬라이싱을 떠올려보세요")
3. 내가 2단계 후에도 명시적으로 요청하면 그때 코드를 보여준다

한 번에 3단계로 건너뛰지 않는다.

## 내 학습 목표

파이썬 관용구를 손에 붙이는 것이 최우선이다. 특히 약한 부분:

- 음수 인덱싱, 슬라이싱 (`lst[-1:]`, `lst[::2]`, `s[a::b]`)
- `enumerate` (특히 `start=1`)
- 딕셔너리 매핑으로 `if-elif` 사슬 대체
- `sorted(key=)`, `min/max(key=)`
- `collections.Counter`, `dict.fromkeys`
- `itertools.combinations`, `permutations`

리뷰할 때는 `for` 문으로 짠 코드를 위 관용구로 압축하는 방향을 **최우선으로** 제안한다.

## 저장소 구조

```
programmers/lv0/<문제명>/
├── problem.md    문제 설명 + 원본 링크 (자동 생성)
├── solution.py   내 풀이 (내가 직접 작성)
├── tests.json    입출력 예시 (자동 생성)
└── note.md       배운 관용구 (내가 작성)
```

## 커밋 컨벤션

- `solve: <문제명> (<레벨>, <정답률>)` — 처음 통과시켰을 때
- `refactor: <문제명> - <적용한 관용구>` — 2차 리팩터링 후

한 문제당 커밋 2개가 정상이다.

## 언어

Python 3. 외부 라이브러리는 표준 라이브러리만 사용한다.
